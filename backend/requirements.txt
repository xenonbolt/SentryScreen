# ── Adverse Media Screening Copilot — Python Dependencies ────────────────────
# Python >= 3.10 required
# AMD ROCm: install PyTorch with ROCm wheels (see README)

# Web framework
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.12
streamlit==1.41.1

# Configuration
python-dotenv==1.0.1

# Data validation
pydantic==2.10.3
pydantic-settings==2.6.1

# ML / NLP — core
# NOTE: For AMD MI300X, install PyTorch with ROCm:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/rocm6.2
torch>=2.1.0
sentence-transformers==3.3.1
transformers==4.47.1
tokenizers>=0.19.0

# Vector search
faiss-cpu==1.9.0
# For GPU FAISS on ROCm, build from source or use:
#   pip install faiss-gpu  (if pre-built ROCm wheel available)

# Entity resolution
rapidfuzz==3.10.1

# Optional: LLM summaries via Ollama
httpx==0.28.1

# Optional: Enhanced keyword extraction
# keybert==0.8.5
# scikit-learn>=1.3.0

# Utilities
numpy>=1.26.0
duckduckgo-search>=6.0.0
beautifulsoup4>=4.12.0
requests>=2.31.0

# Article extraction (newspaper3k for clean full-text scraping)
newspaper3k>=0.2.8
lxml[html_clean]>=4.9.0
